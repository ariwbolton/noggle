export default {
    name: 'user.game.get',
    handler: async function(request) {
        // Needs authentication
        // User needs to have been in game at some point
        // Retrieve and return all user information
    }
}
