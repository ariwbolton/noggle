export default {
    name: 'user.get',
    handler: async function(request) {
        // Needs authentication
        // User needs to match
        // Retrieve and return user, with games and short description
    }
}
