export default {
    name: 'room.game.create',
    handler: async function(request) {
        // Needs authentication
        // User needs to be owner of room
        // Create game
        // Add existing users in room to game
        // Return room
    }
}
