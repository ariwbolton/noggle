export default {
    name: 'room.game.assignment.unreadyup',
    handler: async function(request) {
        // Needs authentication
        // User needs to be in room
        // Room needs to have a game which has been initialized but not started
        // User needs to not have already started
        // Delete game assignment (??)
        // Notify other users in room
        // Return success to user
    }
}
