export default {
    name: 'room.game.assignment.word.create',
    handler: async function(request) {
        // Needs authentication
        // Game needs to be active
        // User needs to be active in game
        // Word needs to be on board
        // Word does NOT need to be a "real english" word
        // Create word
        // Return success to user
    }
}
