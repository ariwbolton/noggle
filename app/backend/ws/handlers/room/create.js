export default {
    name: 'room.create',
    handler: async function(request) {
        // Needs authentication
        // User needs to not be in any other rooms
        // Create room
        // Join room
    }
}
