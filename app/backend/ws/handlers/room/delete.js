export default {
    name: 'room.delete',
    handler: async function(request) {
        // Needs authentication
        // User needs to be the owner of the room
        // Remove all users from room, notify them
        // Delete the room, notify owner of deletion
    }
}
