export default {
    name: 'room.invite.create',
    handler: async function(request) {
        // Needs authentication
        // User needs to be the owner of the room
        // Enforce max number of invites (optional)
        // Create invite, notify user
    }
}
