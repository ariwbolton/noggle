export default {
    name: 'room.invite.accept',
    handler: async function(request) {
        // Needs authentication
        // User needs to match the invite
        // Create room assignment
        // Join room
        // Mark invite as accepted
        // Notify others in room
    }
}
