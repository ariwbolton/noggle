export default {
    name: 'room.get',
    handler: async function(request) {
        // Needs authentication
        // User needs to be in the room to get it
        // Retrieve and return room
    }
}
